@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<link rel="stylesheet" type="text/css" href="~/Content/css/profile.css">

<div class="container p-0">
    <div class="row">
        <!-- Sidebar bên trái -->
        <div class="col-md-2" style="background-color: #F0F0F0">
            <div class="list-group mb-5">
                <!-- Profile Section -->
                <div class="text-center py-3">
                    <img src="~/Content/images/logo/fahasa-logo.png" class="rounded-circle" alt="Profile Image" style="width: 100px; height: 100px;">
                    <h5 class="mt-2">vnhyt964</h5>
                    <a href="@Url.Action("Index", "Profile", new { view = "ProfilePartial" })" class="text-secondary">Sửa Hồ Sơ</a>
                </div>

                <!-- Sidebar Menu Items -->
                <a href="@Url.Action("Index", "Profile", new { view = "ProfilePartial" })" class="list-group-item account-item @(ViewBag.PartialView == "ProfilePartial" || ViewBag.PartialView == "AddressPartial" || ViewBag.PartialView == "ChangePasswordPartial" ? "active" : "")">
                    <i class="fas fa-user"></i> Tài Khoản Của Tôi
                </a>

                <!-- Sub-items for Tài Khoản Của Tôi (Always visible) -->
                @if (ViewBag.PartialView != "OrderPartial")
                {
                    <div class="sub-menu">
                        <a href="@Url.Action("Index", "Profile", new { view = "ProfilePartial" })" class="list-group-item sub-item @(ViewBag.PartialView == "ProfilePartial" ? "active" : "")">
                            <i class="fas fa-address-card"></i> Hồ Sơ
                        </a>
                        <a href="@Url.Action("Index", "Profile", new { view = "AddressPartial" })" class="list-group-item sub-item @(ViewBag.PartialView == "AddressPartial" ? "active" : "")">
                            <i class="fas fa-map-marker-alt"></i> Địa Chỉ
                        </a>
                        <a href="@Url.Action("Index", "Profile", new { view = "ChangePasswordPartial" })" class="list-group-item sub-item @(ViewBag.PartialView == "ChangePasswordPartial" ? "active" : "")">
                            <i class="fas fa-key"></i> Đổi Mật Khẩu
                        </a>
                    </div>
                }

                <!-- Divider -->
                <hr>

                <!-- Additional Menu Items -->
                <a href="@Url.Action("Index", "Profile", new { view = "OrderPartial" })" class="list-group-item @(ViewBag.PartialView == "OrderPartial" ? "active" : "")">
                    <i class="fas fa-shopping-bag"></i> Đơn Mua
                </a>
            </div>
        </div>

        <!-- Nội dung bên phải -->
        <div class="col-md-10 p-4">
            @if (ViewBag.PartialView == "ProfilePartial")
            {
                @Html.Action("ProfilePartial", "Profile")
            }
            else if (ViewBag.PartialView == "AddressPartial")
            {
                @Html.Action("AddressPartial", "Profile")
            }
            else if (ViewBag.PartialView == "ChangePasswordPartial")
            {
                @Html.Action("ChangePasswordPartial", "Profile")
            }
            else if (ViewBag.PartialView == "OrderPartial")
            {
                @Html.Action("OrderPartial", "Profile")
            }
        </div>
    </div>
</div>
